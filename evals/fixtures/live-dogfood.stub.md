# Live dogfood fixture (stub)

Not an executable eval. Reminder for agents and future planted fixtures.

- **Target:** a public third-party repo the session does not know. Not `pedroknigge/pstack-skills`. Not a private tree.
- **Act:** run parent `pstack` / a small `ps-*` set cold. Read each `SKILL.md` in full.
- **Record:** pack friction only (routing, refs, stale descriptions).
- **File:** HITL via `scripts/ps-issue.sh` on `pedroknigge/pstack-skills` with labels `dogfood` / `blindtest` plus `bug` or `enhancement`. Search first. No secrets, no private transcripts, no target source dumps.
- **Do not:** push to the target, open issues on the target, or invent a new skill verb.

A later planted fixture can add a toy tree with one deliberate stale `description`. Until then, live dogfood is the real test of the next release.
